{
  "name": "formpath-site",
  "version": "1.0.0",
  "private": true,
  "description": "FormPath JS landing page and examples site.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "pnpm build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "formpath": "^1.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.4.8"
  }
}
